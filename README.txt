
use the script

push.dsh.jobs.py

to run many jobs on multiple machines and cores. 

the push.qsub.jobs.py script was created for a Sun cluster that uses qsub.

runMethod.R is the very entry Code and the main MCMC loop happens in Code/main.R
