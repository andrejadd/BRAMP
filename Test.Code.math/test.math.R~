
##
## This if for testing to see if my code does the same as Marcos
##

total.parents = 11  # 9 + 2 additional

parents = c(1,2,3)

## init a [parents,parents] matrix
Cov_mat = matrix(c(1:parents))

## extract the parents for this node (the rows i and columns j, for all (i,j) covariances)
Cov_mat = 

X = matrix( c(1.0, 1.1, 2.1,
              1.0, 1.2, 2.2,
              1.0, 1.3, 2.3,
              1.0, 1.4, 2.4,
              1.0, 1.5, 2.5), nrow=3, ncol=5)

mu_vec = c(1,11,21)

mu.tilde = t(X) %*% mu_vec

inv.Sigma.tilde = diag(n.obs) - t(X) %*% inv(inv(delta.snr * Cov_mat) + X %*% t(X)) %*% X

